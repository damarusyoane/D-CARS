import { useAuth } from '../../contexts/AuthContext';

/**
 * Simple component to debug authentication state
 */
// AuthDebug overlay removed as per user request.
export default function AuthDebug() {
  return null;
}
